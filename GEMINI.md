# DX Terminal — Gemini Overlay

Follow `AGENTS.md` as the shared contract.

Gemini-specific expectations:
- use the same VDD lifecycle, documentation sources, and dx MCP bridge as every other runtime
- avoid creating Gemini-only workflow state when shared project state already exists
- keep outputs aligned with dashboard-visible project progress and acceptance evidence

If this file and `AGENTS.md` diverge, update the shared guide first.
