# DX Terminal — Codex Overlay

Follow `AGENTS.md` as the shared contract.

Codex-specific expectations:
- use local skills when present under `.codex/skills` or `~/.codex/skills`
- prefer precise code edits and verification over narrative output
- treat the dx gateway as the MCP access point for external tooling

If this file and `AGENTS.md` diverge, update the shared guide first.
