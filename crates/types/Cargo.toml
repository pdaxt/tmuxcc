[package]
name = "agentos-types"
version = "0.1.0"
edition = "2021"
description = "Shared types for AgentOS micro MCPs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
